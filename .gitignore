.superpowers/
.worktrees/
node_modules/
dist/
*.pid

# Local-only config and durable memory (keep private business memory out of git)
.env.local
/memory/

# Whisper local runtime (keep server.py + transcribe.py; ignore venv/models/samples)
.whisper/.venv/
.whisper/__pycache__/
.whisper/*.wav

# Local scratch areas
work/*
!work/.gitkeep
outputs/*
!outputs/.gitkeep

# OS/editor noise
.DS_Store
Thumbs.db
